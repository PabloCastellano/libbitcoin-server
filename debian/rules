#!/usr/bin/make -f

%:
	dh $@ --with autoreconf,quilt

override_dh_auto_configure:
	dh_auto_configure -- --without-tests

override_dh_shlibdeps:
	dh_shlibdeps --dpkg-shlibdeps-params=--ignore-missing-info
